// forcea array to be number types
const arr: number[] = []

arr.push(1)

//Tuple, fixed length array with types
// use ? to make the arguments optional
type MyList = [number?, string?, boolean?]

const arr1: MyList = []